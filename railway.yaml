services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - 3000
    env:
      PORT: 3000
    watch:
      - backend/
      - Dockerfile.backend
      - railway.yaml
