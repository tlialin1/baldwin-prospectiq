services:
  - type: web
    name: baldwin-backend
    runtime: python
    plan: free
    buildCommand: "echo 'Build complete'"
    startCommand: python backend/server.py
    envVars:
      - key: PORT
        value: 10000
