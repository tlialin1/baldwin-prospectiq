# Frontend2 - React Version

This is the React version of the Baldwin Dashboard.

To deploy on Render:
1. Create a new Web Service
2. Name: baldwin-frontend2
3. Runtime: Node
4. Build Command: npm install && npm run build
5. Start Command: npx serve -s build -l 10000
6. Add PORT=10000 environment variable
